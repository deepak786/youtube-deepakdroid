# The 384 numbers, on a napkin

Real embeddings are **384 dimensions** in this demo.

Some models use thousands.

We cannot draw that.

This 2D picture is only to see the idea: nearby = similar meaning.

```text
● stripe-webhook-retries.md


        ● card-processor-latency.md


                    ● cart-session.md


                              ●  "app freezes on payment"
                              ●  checkout-timeout.md
```

The query sits next to `checkout-timeout.md`.

That is why vector search scored it **0.415** — and why keyword scored nothing.
